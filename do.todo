Backend (Framework):
☐ en los routers vista agrega el body osea por ejemplo los put y post deben tener un formato
de ejemplo esto debe aparecer pero para evitar romper el diseño esto debe haber un flecha para arriba y para abjao
que muestre un ejemplo del body, el body debe estar comentado para poder obtener rapidamente el ejemplo
☐ Las variables de tu ogDb se conectan creo que constante, esto debe cambiar lo debes inicializar
la contencion en un lado conigurable tipo consts.php para que pongas alli la de en este caso wordpress
o la de standalone, con eso ya se puede variar.s
☐ COmo AUTH maneja tablas de user que esta en App entonces debe ver como los unes o como se hace alli
porque esta que usa la constante DB_TABLES entonces esta mal o no tan mal, pero digamos esa variable esta
regenerica para usarlo en el framework
☐ que en handler, controlller, helpers se pueda ir dentro de carpetas,
creo que actualmente solo si lo pones asi ->helper('algo') el va buscar dentro de
la carpeta helpers, pero si quiero poner helpers/mis_helpers/mi_helper.php
no lo encuentra, esto debe ser posible, asi como en controllers y handlers
para eso deberia tomar encuenta el ultimo slash / y tomar todo lo que este antes como la carpeta
y lo que este despues como el archivo a cargar.

Frontend (Framework):
☐ el css debe usar el formato de og- ogToast para que no haya conflictos con otros plugins
☐ Hooks a nivel de plugins, hay que hacer el sistema para poder parsear todos los hooks
de un plugin registrado para cuando se carguen las vistas, form, mdoa, etc... 
se agreguen los hooks en los lugares correspondientess
☐ Hooks a nivel de menu, no existe hooks a nivel de menu por ejemplo que un plugin
agregue un menu a otro o algo asi.
☐ efecto al momento de refrescar la table, admin.js hace un rerfesh pero el componente no tiene aun un efecto
☐ Access por mensaje o visualizacion el contexto del permiso algo como role_context : visible|denegado
debeciones por vista, por ejemplo si quiero que dentro de una vista ese usuaro no pueda crear algo enton valdiarse eso dentro del componente permisos de usuario alli, entonces digamos ese boton debe tener como una espcie de data-attribute
o que tipo de permiso es ese booton o elemeto y debe consultar alli si el usuario tiene permiso o no}
☐ agregar mejoras en la tablas, como lo de la columnas visibles
☐ Repetibles con drop and drop para ordenar los elementos
☐ Repetibles tipo acordions
☐ Hacer que la tambien se pueda enrolar las tab de tabs para rol tipo 'admin' entonces
una persona normal no podria ver X tab pero si un admin.
☐ persistencia en widgets posicion
☐ grouper tabs no se ve los estilos de degradados como lo hace tabs en responsive scroll horizontal
☐ El type submit el label de json debe ser su value para que cambie el txto del boton
actualmente si pones un type : "submit" crea un boton con el texto "enviar" y label se coloca
en una extraña posicion en si.
☐ crear el ejemplo de un menu de 3 niveles porque si lo hace
☐ en react native quedo pendiente seguir con el tema de la navegacion
link https://claude.ai/chat/ae42cc5a-dcf4-4691-957c-c4e494d42a9c
☐ sgun esta conversacion el "action": "submit:auth.login" esta generando otro evento y se duplica en el submit
almenos en el login hay que preguntar si esto va duplicar al resto deformularios
☐ Agregar un tipe "tag" que es un componente que permita agregar multiples etiquetas con la "," o tecla ENTER se van separando
esto sirve para ponerlo en los activadores de bienvenida del infoproducto
☐ Agregar que tu puedes tener un campo que solo sea visible para el admin
esto va permitir listar todos los usuarios cosa que por FORM tu puedes poner
a quien le pertenece ese registro, por defecto estara el user logeado pero el admin puede cambiarlo
y poner otro usuario. Con esto podemos rapidamente crear registros para otros usuarios.
Otra cosa, el admin debe tener la posibilidad de ver todos los registros de todos los usuarios
actualmente solo ve los suyos. Esto debe ser algo estandar en todo el sistema.
(PARECE QUE ESTO YA EXISTE CON ROLE ADMIN EL CAMPO SE MUESTRA O NO, SOLO FALTARIA LLENARLO CON UN ENDPOINT
Y VALIDAR EN BODY DEL JSON QUE SI ESTA DISPONIBLE PUES ACTUALIZAR EL user_id)
☐ si un formularo tiene un campo obligatorio, entonces el scroll debe llenar hasta alli para verlo y el focus debe 
quedar en el primer campo obligatorio que falte por llenar
☐ Parte los JSON por ejemplo existe JSON muy extensos entonces puede que se partan en viaras partes
y que haya un item de tipo:json_part que cargue esa parte del json en especifico llamando no mas la ubicacion
y asi opdemos partir las vistas , porque por ejemplo la vista de CRUD de producto es muy grande en realidad esta consumiento 
michos tokens.

